# DEVLOG

## 2026-08-29: 盤面レイアウト・画面遷移・初期配置の改善確認

### 概要
実機確認で見つかった盤面レイアウトのズレ、決定ボタンの重なり、ゲーム開始前の画面遷移、起動時の初期画面、犯人側の初期車配置表示を確認・整理した。あわせて、ゲーム中のプレイモード表示を追加した状態を記録する。

### 変更内容と修正理由

#### 1. 盤面レイアウトのズレ修正
- **問題**: 5×5のビル配置で、盤面外周のビルと内側のビルの間隔がずれて見えていた。
- **原因**: `board_widget.dart` で `boardPixelSize - edgeInset * 2` と自前計算していた `innerSize` が、BorderとPaddingを含めてFlutterが実際に確保する描画領域と一致していなかった。
- **修正**: Padding内部を `LayoutBuilder` で取得し、`constraints.maxWidth` を実際の `innerSize` として使用するよう統一した。
- **実装上のポイント**: ビル、道路、交差点、街装飾のレイアウト計算基準を、Padding内部の実描画領域に合わせた。
- **実機確認済み**: 外周と内側のビル間隔のズレが解消し、正常動作していることを確認した。

#### 2. 決定（OK）ボタンの配置
- **問題**: ヘリコプターや車の移動先を選択した後に表示される決定ボタンが、盤面と重なって見えることがあった。
- **確認した実装**: `pendingActionBarHeight` と `_buildPendingActionBar` により、盤面の下に固定高さの領域を確保している。
- **実機確認済み**: 決定ボタンの位置に問題はなく、盤面との重なりもないことを確認した。現状を維持する。

#### 3. ゲーム中のモード表示
- **変更**: City Chaseの表示付近に、現在のモードとして「1人プレイモード」または「2人対戦モード」を追加した。
- **実装上のポイント**: 箇条書きのように見える「・」などは使わず、City Chaseより少し小さい文字サイズで表示している。
- **実機確認済み**: 正常に表示されることを確認した。

#### 4. ゲーム開始前の戻るボタン
- **変更**: タイトル → 遊び方選択 → 役割選択 → 難易度選択 → ゲームの各画面で、一つ前の画面へ戻れるようにした。
- **実装上のポイント**: 戻るボタンはCity Chaseのタイトル部分に組み込まず、独立したボタンとして配置した。タイトル文字の固定位置・既存の色・デザインを維持し、戻る操作を独立したUIとして扱っている。
- **実機確認済み**: 各画面から一つ前の画面へ戻れることを確認した。

#### 5. 起動時にタイトル画面へ進まない問題
- **問題**: `GamePage` の `initState()` で `_startNewGame()` を呼ぶと、起動時に `GamePhase.roleSelect` へ進んでしまっていた。
- **修正**: 起動時は `_resetBoardState()` を使用し、初期値の `GamePhase.title` を維持するようにした。
- **実機確認済み**: アプリ起動時にタイトル画面から開始することを確認した。

#### 6. 犯人側の初期車配置
- **問題**: 初期車配置でビルを選択した後、確認ダイアログ表示中に選択したマスが分からなかった。
- **修正**: `pendingCarRow` と `pendingCarCol` を使い、確認中のビルを一時的にハイライト表示するようにした。
- **実機確認済み**: 選択したマスが分かるようになり、正常動作することを確認した。

#### 7. 警察側ヘリ初期配置のリセット修正
- **問題**: 警察側のヘリコプター配置で、確認ダイアログの「戻る」を押したときに、最後に配置した1機だけが取り消されてしまい、ヘリ1からやり直せない状態だった。
- **原因**: 既に配置済みのリストから `removeLast()` だけを実行していたため、前の2機が残ったままになっていた。
- **修正**: 配置済みヘリ3機をまとめて初期化し、`Helicopter.resetInitialSetup()` を使って一覧をクリアするよう変更した。
- **結果**: 「戻る」を押したあとは、ヘリ1から再度選択を開始できるようになった。
- **補足**: ログにも「ヘリ1から再配置してください」と明示し、ユーザーが次の操作を理解しやすくした。

### コードとの照合結果
- 今回記録した6項目について、現行実装との明確な相違点は見つからなかった。
- `app/lib/views/board_widget.dart` では `LayoutBuilder` の `constraints.maxWidth` が実描画領域として使われている。
- `app/lib/views/game_page.dart` では、起動時の `_resetBoardState()`、モード表示、戻る処理、決定バーの固定領域、初期車配置の候補状態が実装されている。
- 実機上の表示結果そのものはコードだけでは再検証できないため、上記の実機確認は今回の確認結果として記録する。
- 静的解析は完了した。既存コード由来の非推奨API・未使用要素などの情報/警告はあるが、今回の対象に関するコンパイルエラーは確認されなかった。`flutter test` は全7テスト成功した。

### 未確認・保留事項

#### 1. 画面遷移時の表示崩れ・カクつき
- 最近、「ガビガビ」「亀裂」のように見える表示崩れ・カクつきが顕著になっている。
- Flutter Webの描画、画面遷移、アニメーションなどが原因の可能性があるが、原因は未確定である。
- `flutter run -d chrome --web-renderer html` は試行したが、現在のFlutterでは `--web-renderer` オプションが存在せずエラーになった。
- 原因調査・修正は次回以降とし、今回は変更しない。

#### 2. ビルと道路の間の隙間
- 現在はビルと道路が近いため、少し余白を設ける方向で検討している。
- ChatGPTとの比較でB/C/D案を検討中。D案は「盤面を少し拡大し、B程度の隙間を設ける」案で、隙間の方向性は好評だった。
- 最終的な数値とデザインは未確定のため、今回は実装しない。

#### 3. ログの座標表記
- 現在のビル座標は直感的に分かりにくい。
- A1〜E5などの座標表記、盤面への座標ラベル表示、ビル番号の付与を候補として検討中。
- 仕様未確定のため、変更しない。

#### 4. 建物・町並みのデザイン
- 現在の盤面はゲームとしては見やすいが、実際の日本の町並みとは異なる。
- 将来的に「日本の地方都市をベースにした、ゲームとして分かりやすいデフォルメされた町並み」の方向性を検討する可能性がある。
- デザイン仕様未決定のため、変更しない。

### 次回予定
- 画面遷移時の表示崩れ・カクつきの再現条件と原因を調査する。
- ビルと道路の隙間について、B/C/D案を比較して最終値を決める。
- ログ座標表記の仕様を決定する。
- 日本の地方都市を意識した町並みデザインの方向性を検討する。

## 2026-08-09: コード破損からの復旧とプロジェクト構造の一元化

### 概要
コード編集時の重複・破損トラブルから、Claude版の11ファイル分割コードを用いてゲーム動作の完全復帰を達成。
あわせて、これまで分離していた仕様書管理リポジトリ（`project-Chase`）の `app/` ディレクトリ配下にアプリ本体コードを統合・Push完了。

### トラブルと解決の経緯
1. **課題の発生**
   - ファイル内でのコード重複・欠損により、ビルドエラーおよびアプリの動作不良が発生。
   - リポジトリ管理（Git）とローカル環境でディレクトリ階層の不一致があり、コミット範囲の誤認リスクが存在していた。

2. **復旧手順**
   - 事故前の正常な挙動を持つ11ファイル分割構成のコードを採用。
   - `lib/` 配下の構造（`ai/`, `models/`, `views/`）を整理し、ローカルでの正常動作を再確認。

3. **リポジトリへの一元管理化**
   - `project-Chase` のルート直下に `app/` フォルダを配置し、アプリコード一式および標準の `.gitignore` を格納。
   - リモート（GitHub）との競合（コンフリクト）をマージコミットで解消し、`git push origin main` を完了。

### 学び・今後の運用ルール
- **1実装・1コミットの徹底**: 小さな単位でコミット・Pushを行い、何かあってもすぐに以前の正常な状態へ戻せるようにする。
- **作業前後の同期手順**: 作業開始前に `git pull`、作業完了後に `git add .` → `git commit` → `git push` の基本フローを必ず守る。


## 2026-08-06

- 役割選択を実装
- 警察AIを追加
- AI管理体制を整備
- GitHubとObsidianの運用開始

次回
- 捜索済みマーカー
- 痕跡表示改善


## 2026-08-05

### 今日の目標
Project Chaseの開発環境とプロジェクト管理体制を整え、MVP開発を継続できる状態にする。

### 完了したこと

#### 開発環境
- Flutter開発環境を構築
- Android Studioを導入
- GitHubリポジトリを用意
- Obsidianによるドキュメント管理を開始

#### ゲーム開発
- 役割選択（警察／犯人）を実装
- 警察AI・犯人AIを改善
- ゲームルールを公式ルールに近づけて調整
- ログ表示・盤面サイズなどUIを改善

#### プロジェクト管理
以下のドキュメントを作成・整備

- SPECIFICATION.md
- TASKS.md
- CHANGELOG.md
- AI_GUIDE.md
- AI_PROJECT_MANAGER.md
- CURRENT_STATUS.md

#### AI運用
- ChatGPT・Claude・Geminiで役割分担する方針を決定
- ObsidianをAI共通の情報管理場所として採用
- AI同士が引き継ぎしやすい開発フローを整備

### 決定事項

- Project名は「Project Chase」
- 完成を最優先にする
- MVP完成後に機能追加を行う
- Flutter Webをベースに開発を進める
- 将来的にAndroid・iPhone・オンライン対戦へ対応予定
- GitHubでソースコード管理を行う

### 気づき

ドキュメント管理の仕組みが整い、AIを切り替えても開発を継続しやすい体制になった。

### 次回やること

- 捜索済みマーカーの実装
- 痕跡表示の改善
- 捜索アニメーション追加
- ゲームバランスの調整

### 一言

Project Chaseの本格的な開発体制が完成した一日。
今後はドキュメント作成よりもゲームの完成を優先し、プレイできるバージョンを育てていく。