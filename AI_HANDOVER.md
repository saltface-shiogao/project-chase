# AI_HANDOVER.md

> Project ChaseのAI・チャットルーム・担当AIが交代する際に、直前の作業状態から安全に再開するための引き継ぎ専用ファイル。
>
> このファイルは正式な仕様書ではありません。既存のProject Chaseドキュメントを補完し、「今この瞬間の作業状態」を引き継ぐことを目的とします。

---

## 1. 引き継ぎ情報

- 最終更新日時：2026-08-09
- 最後に作業したAI：ChatGPT
- 作業環境：Windows 11 / VS Code / Flutter Web（Chrome）
- 現在の作業段階：復旧確認完了・GitHubバックアップ整備前
- 引き継ぎ状態：**正常動作確認済みのClaude分割版を基準候補として保全する段階**

---

## 2. 現在の作業

### 現在取り組んでいるタスク

Project Chaseのコード破綻から復旧し、今後AIが交代しても安全に開発を継続できる運用を整える。

直前の作業では、Claudeによるコード分割の途中でClaudeの利用制限が発生したため、その途中状態をGeminiへ引き継いだ。

Geminiによる変更後、ゲームは起動したものの、以下の意図しない変更が発生した。

- 5×5盤面が崩れた
- ヘリコプターの挙動がおかしくなった
- ターン数がおかしくなった
- ゲームルールが変化した

その後、Claudeが作成したコード分割版を別環境へ配置し、Flutter Webで動作確認した結果、ゲームが正常に復活していることを確認した。

### 現在の段階

**復旧確認済み。次は正常動作確認済み状態をGitHubへ保存する段階。**

---

## 3. 直前に行った変更

### Claudeによるコード分割

元の `lib/main.dart` が大きくなったため、ゲームの挙動・ルールを変更しない条件でコード分割を実施。

Claudeから提示された配置：

```text
lib/
├── main.dart
├── models/
│   ├── player_role.dart
│   ├── game_phase.dart
│   ├── helicopter.dart
│   └── police_ai_action.dart
├── ai/
│   ├── criminal_ai.dart
│   └── police_ai.dart
└── views/
    ├── role_select_view.dart
    ├── log_panel.dart
    ├── board_widget.dart
    └── game_page.dart
```

Claudeの説明では、ゲームロジックは変更せず、既存の犯人AI「逃げ道優先」ロジックと警察AI「未捜索優先」ロジックを `ai/` へ移動した。

### Geminiによる変更

Claudeの利用制限後、途中状態からGeminiへコード修正を依頼。

その結果、ゲームの挙動・盤面・ヘリコプター・ターン数・ルールに意図しない変更が発生した。

---

## 4. 現在の状態

### 正常動作を確認済み

Claudeが作成したコード分割版を、元の作業環境とは別の検証用コピーに配置。

検証環境：

```text
ProjectChase_ClaudeRecovery\ProjectChase
```

VS Codeで開き、以下を実施：

```bash
flutter run -d chrome
```

結果、ゲームが正常に起動した。

確認済み：

- ゲーム起動
- 5×5盤面
- 犯人／警察の役割選択
- ゲームが正常に復活していること

### 未確認

今回の復旧確認では、以下を完全には検証していない。

- 全11ラウンドを通した完全な動作
- 全勝敗条件
- 全AI挙動
- 全UI状態
- Android実機での動作
- iOSでの動作
- オンライン対戦関連機能
- Claude分割版と分割前正常版の完全な回帰テスト

### 現在の問題

復旧確認用のClaude版について、現時点で具体的な実行エラーは確認されていない。

ただし、完全な回帰テストを終えたわけではないため、**「仕様を100%維持していることが確認済み」とは扱わない。**

### 現在わかっていないこと

- Gemini変更前後の完全なコード差分
- Claude分割版と分割前の正常版の完全な差分
- Claude分割版の全仕様に対する完全な回帰テスト結果
- GitHubへの最新バックアップ状態

不明な情報は推測しない。

---

## 5. 最後に正常だった状態

今回の事故以前に正常だった状態：

- 5×5マスの盤面
- 犯人／警察を選択可能
- ヘリコプターが正常に動作
- ターン数が正常
- ゲームルールが正常
- ゲーム自体が起動・プレイ可能

その後、Claudeが「ゲームの挙動やルールを変更しない」という条件でコードを分割した。

Claude版はClaude自身による最終確認前に利用制限が発生したため、当初は正常版と断定できなかった。

その後、Claude分割版を別フォルダに配置してFlutter Webで実行し、上記の基本状態が正常であることを確認した。

### 現在の基準候補

```text
ProjectChase_ClaudeRecovery\ProjectChase
```

**この状態を「正常動作確認済みの基準版候補」として扱う。**

GitHubへの保存が完了するまでは、不要な変更を加えない。

---

## 6. バックアップ / Git状態

### 現在

- GitHubでゲームコードのバックアップ：未確認
- 正常動作確認済みClaude版のGitHub保存：未実施
- ローカルの復旧用コピー：あり
- 正常動作確認済み基準候補：あり

### 重要

今回の事故では、ゲームコードのGitHubバックアップを事前に設定していなかったため、Geminiによる変更後に簡単に以前の状態へ戻せなかった。

今後は、**AIによるコード変更前に復旧可能な状態を作ることを基本ルールとする。**

GitHubへ保存するまでは、正常動作確認済み基準候補を大きく変更しない。

---

## 7. 次にやること

優先順位：

### 最優先

1. 現在の正常動作確認済みClaude版を保全する
2. 既存のGitHubリポジトリ `project-Chase` の現在の構成を確認する
3. 既存のObsidian仕様書を壊さず、ゲームコードを同じリポジトリで管理する方法を決める
4. 正常動作確認済みClaude版をGitHubへ保存する
5. 復旧可能なセーブポイントを作る

### その後

6. GitHubでのバックアップ・コミット運用を整える
7. AI切り替え時に `AI_HANDOVER.md` を更新する運用を開始する
8. 必要に応じて `CURRENT_STATUS.md`、`TASKS.md`、`DEVLOG.md`、`CHANGELOG.md` 等を更新する
9. 安全なセーブポイントを確保した後、通常の開発へ戻る

---

## 8. 次のAIが最初にすること

次のAIは、いきなりコードを変更しない。

1. `AI_HANDOVER.md` を読む
2. `AI_GUIDE.md` を確認する
3. `AI_PROJECT_MANAGER.md` を確認する
4. `CURRENT_STATUS.md` を確認する
5. 必要に応じて `SPECIFICATION.md`、`DECISIONS.md`、`TASKS.md`、`DEVLOG.md`、`CHANGELOG.md`、`ROADMAP.md` を確認する
6. 現在のコードの状態を確認する
7. GitHub / バックアップ状態を確認する
8. 現在の正常動作確認済み基準候補を確認する
9. 作業目的と変更範囲を明確にする
10. 不明点があれば推測せず、ユーザーへ確認する
11. 必要なら変更案を先に提示し、承認を得てから変更する

---

## 9. 次のAIがやってはいけないこと

今回の事故を防ぐため、以下を原則とする。

- 既存仕様を勝手に変更しない
- 指示された作業範囲を超えて変更しない
- 大幅なコード変更を一度に行わない
- 不明点を推測して実装しない
- 作業途中のコードを「完成版」と判断しない
- AIが途中で交代した場合、前AIの作業内容を確認せず修正を続行しない
- 複数ファイルを変更する場合、変更理由と対象ファイルを明確にする
- 正常動作確認済みの状態を、バックアップなしで上書きしない
- ゲーム仕様を変更する場合は、コード変更前にユーザーへ確認する
- リファクタリングを理由にゲームの挙動・ルール・UIを変更しない
- 回帰テストを実施したことを確認できない状態で「問題なし」と判断しない

---

## 10. 変更時の安全ルール

コードを変更する前に、最低限以下を確認する。

```text
① 現在の正常状態を確認
        ↓
② バックアップ / Gitコミットを確認
        ↓
③ 変更対象ファイルを確認
        ↓
④ 変更内容と目的を明確化
        ↓
⑤ 必要ならユーザー承認
        ↓
⑥ コード変更
        ↓
⑦ 動作確認
        ↓
⑧ 結果を記録
        ↓
⑨ AI_HANDOVER.mdを更新
```

特に大きなリファクタリングでは、**「変更前の正常状態」と「変更後の正常状態」を別々に確認する。**

---

## 11. 既存ドキュメントとの役割分担

`AI_HANDOVER.md` は既存ドキュメントの代替ではない。

- ゲーム仕様 → `SPECIFICATION.md`
- プロジェクト全体の状態 → `CURRENT_STATUS.md`
- 決定事項 → `DECISIONS.md`
- 作業履歴 → `DEVLOG.md`
- 変更履歴 → `CHANGELOG.md`
- タスク → `TASKS.md`
- 長期計画 → `ROADMAP.md`
- AIへの基本ガイド → `AI_GUIDE.md`
- AIによるプロジェクト管理方針 → `AI_PROJECT_MANAGER.md`
- **直前の作業状態・AI交代時の引き継ぎ → `AI_HANDOVER.md`**

既存ドキュメントの内容を `AI_HANDOVER.md` に詳細に複製しない。

`AI_HANDOVER.md` に必要なのは、

> 「次のAIが今の作業を安全に再開するために必要な情報」

だけとする。

---

# AI引き継ぎ手順

## 現在のAI

1. 作業を終了する
2. `AI_HANDOVER.md` を最新状態に更新する
3. 「現在の作業」「完了状況」「未確認事項」「問題」「最後に正常だった状態」を確認する
4. 必要に応じて `CURRENT_STATUS.md`、`TASKS.md`、`DEVLOG.md`、`CHANGELOG.md` 等も更新する
5. コード変更前のバックアップ / Gitコミットを確認する
6. 次のAIへプロジェクトと `AI_HANDOVER.md` を渡す

## 次のAI

1. まず `AI_HANDOVER.md` を読む
2. 既存の関連ドキュメントを確認する
3. 現在のコードとバックアップ状態を確認する
4. 「最後に正常だった状態」を確認する
5. 現在の作業目的を確認する
6. 不明点があれば推測せず確認する
7. 必要な変更範囲を明確にしてから作業する
8. 作業後に動作確認する
9. 作業終了時に `AI_HANDOVER.md` を更新する

---

## 更新ルール

このファイルは、AIが交代するときだけでなく、**重要な作業状態が変化したときにも更新する。**

特に以下の場合は更新する。

- コード変更を完了した
- コード変更が途中になった
- 動作確認を完了した
- エラーが発生した
- エラーを解消した
- 正常なセーブポイントを作った
- GitHubへコミットした
- AIを交代する
- 次のAIへ作業を渡す

不明な情報は推測せず、

- `未確認`
- `情報なし`

と明記する。
