# AI_HANDOVER.md

> Project ChaseのAI・チャットルーム・担当AIが交代する際に、直前の作業状態から安全に再開するための引き継ぎ専用ファイル。
>
> このファイルは正式な仕様書ではありません。既存のProject Chaseドキュメントを補完し、「今この瞬間の作業状態」を引き継ぐことを目的とします。
> ゲーム仕様・全体状態・タスク・決定事項・作業履歴・変更履歴・長期計画・AI運用ルールについては、それぞれ該当ドキュメント（11章参照）を優先し、本ファイルでは重複記載しません。

## 1. 引き継ぎ情報

- 最終更新日時：2026-08-29
- 最後に作業したAI：このチャットのAI（最新の更新時点）
- 作業環境：Windows 11 / VS Code / Git / GitHub CLI / Flutterプロジェクトの作業中。
- 重要：別のAIチャットで修正・追記が行われている可能性があるため、**このファイルの情報だけを鵜呑みにせず、未確認項目は必ずユーザーへ確認する**。
- 現在確認できている範囲：
  - 警察側ヘリコプター配置時の「戻る」で、配置済みの3機を全てクリアしてヘリ1からやり直せる修正が記録済み
  - その修正は `TASKS.md` / `CHANGELOG.md` / `DEVLOG.md` に反映済み
  - ただし、**他のAIチャットでも別の修正が入っている可能性がある**ため、コードの最終状態は必ず確認を優先する

## 2. 現在の作業

### 現在確認できている修正内容

1. **警察側ヘリ初期配置のリセット修正**
   - 「戻る」を押したときに、最後に配置した1機だけではなく、配置済みの3機をまとめて初期化してヘリ1から再配置できるようにした
   - 修正後の状態は `TASKS.md` / `CHANGELOG.md` / `DEVLOG.md` に記録済み
2. **画面レイアウトの見直し**
   - ログパネルを盤面の左側へ移動
   - ゲーム中・ゲーム終了時を通じて盤面の表示位置を固定
   - ゲーム終了時の結果表示を「画面上部の結果バナー」方式に変更
3. **ログの全文表示**
   - ログ1行が省略記号で途切れる問題を修正し、折り返し表示に変更
4. **捜索済みマーカーの仕様確定**
   - ヘリごとに直近の捜索場所を管理し、最大3箇所の表示に対応
5. **警察ヘリの操作性改善**
   - ヘリの順序自由化
   - 行動済みヘリの表示改善
   - 確定／キャンセル方式の採用

### 重要な注記
他のAIチャットで修正や再設計が入っている可能性があるため、**このファイルに書いてある内容が最新とは限らない**。特に、直前の修正や未反映のファイルがあれば、次のAIは必ずユーザーに確認してから作業を進める。

### 現在の段階
現時点では、記録済みの修正と未確認の別チャット修正が混在している状態である。**次のAIは、まずユーザーに「どこまでが反映済みか」を確認してから、コードとドキュメントの整合性を取る**こと。

## 3. 直前に変更したファイルと変更内容

いずれも `lib/views/` 配下、UIレイアウト・操作性・表示のみの変更。ゲームロジック（AI判断・移動判定・捜索判定・勝敗判定等）は変更していない。

- **`lib/views/log_panel.dart`**
  - `width` / `height` を外部指定可能なプロパティとして追加（呼び出し側が固定値で渡す想定。パネル自体はゲーム中サイズ変更されない）
  - 各ログ行の `maxLines: 1` + 省略記号表示をやめ、折り返し表示に変更（全文表示のため）
- **`lib/views/board_widget.dart`**
  - 捜索済みマーカー表示ロジックを、単一座標（`lastSearchedRow`/`lastSearchedCol`）からヘリごとの配列（`lastSearchedByHeli`：要素数3、インデックス=id-1）に変更。最大3箇所同時表示に対応
  - 移動/捜索モードの概念を廃止し、確定前候補（`pendingRow`/`pendingCol`/`pendingIsSearch`）のハイライト表示（オレンジ）、捜索可能範囲の常時表示（青）、移動可能な交差点の常時ハイライト（緑リング）を追加
  - ヘリの行動済み表示（半透明＋チェックマークバッジ）を追加
- **`lib/views/game_page.dart`**
  - `build()`内のレイアウトを変更（ヘッダー／結果バナー切替の固定高さ領域、ログパネル左配置、盤面固定位置）
  - `isSearchMode`（モード切替フラグ）を廃止し、`pendingRow`/`pendingCol`/`pendingIsSearch`（確定前候補の保持）を追加
  - タップ処理（`_onBuildingTap`/`_onIntersectionTap`）を「候補選択→確定ボタンで実行」方式に変更
  - ヘリ選択処理（`_selectHelicopterToOperate`）を追加し、未行動ヘリを自由に選択可能に
  - `_confirmPendingAction`/`_cancelPendingAction`を追加
  - `lastSearchedRow`/`lastSearchedCol` → `lastSearchedByHeli`（ヘリごとの配列）に変更
  - レイアウト定数を調整：`controlAreaHeight`を84→96px（確定/キャンセルボタン分の余白確保。値自体はゲーム中固定のまま）、`logPanelWidth`を220→260px

変更していないファイル：`main.dart` / `models/*` / `ai/criminal_ai.dart` / `ai/police_ai.dart` / `views/role_select_view.dart`

## 4. 動作確認結果

**しおがおさんによる動作確認済み。** 今回のセッションで実装した内容（3章）全般について、「完璧で素晴らしい」との評価をいただいた。個別の不具合報告は無し。

なお、Claude側では実行環境（`flutter run`等）を持たないため、コードの妥当性はしおがおさんの手元での実機確認によって担保されている（これは本セッションに限らず、今後のAI引き継ぎでも同様の前提）。

## 5. 未確認事項

- 別のAIチャットで、今回のワークスペースに未反映の修正が入っていないか
- 現在の `AI_GUIDE.md` / `AI_PROJECT_MANAGER.md` / `CURRENT_STATUS.md` / `SPECIFICATION.md` / `DECISIONS.md` / `CHANGELOG.md` / `DEVLOG.md` / `TASKS.md` / `ROADMAP.md` の最新内容
- GitHub上の最新状態とローカルの状態が一致しているか
- どの修正が「現在の正式版」かを、ユーザーから確認できているか
- 今回のヘリリセット修正が、他のAIチャットで追加された別修正と衝突していないか

## 6. 現在発生している問題／エラー内容

**情報なし。** しおがおさんから不具合の報告は無く、現時点で既知の問題はない。

## 7. 最後に正常だった状態

3章に記載の変更一式を反映した状態が、しおがおさんによって動作確認済み・良好と評価されている（2026-08-12時点）。この状態を基準として、次のタスク（8章）に進んで差し支えない。

## 8. 次にやること

### 次のタスク（着手前）：ローカル2人プレイ（Pass & Play）モード

しおがおさんより、次に「ローカルで2人が同じ端末を使って対戦できるモード」を作りたいとの要望あり。ロードマップのフェーズ6（マルチプレイ・対戦機能の実装）に相当するタスクだが、**具体的な仕様はまだ何も決まっていない（未確認）**。

次のAIが着手する前に、しおがおさんとすり合わせが必要と思われる論点の例（あくまで例であり、次のAIが独自に仕様を決めてはいけない）：

- 現在の「役割選択（警察役／犯人役、片方は必ずAI）」画面をどう変更するか（例：「2人プレイ」という第3の選択肢を追加するのか）
- 手番交代の方法（例：片方が操作している間、もう片方に画面を見せない「Pass & Play」的な目隠し操作が必要か、それとも単純に同じ画面を交互に操作するだけでよいか）
- 警察側のヘリ操作（3機）を2人プレイでどちらが操作するか（現状「複数人いる場合は相談しながら操作」という想定が公式ルールにあるが、デジタル版でどう扱うかは未確定）
- 既存の`PlayerRole`（police/criminal）や`GamePhase`など、現在の状態管理モデルへの影響範囲

### その後（フェーズ5内の継続候補・未着手）

- 警察AIの知能強化（発見済み痕跡から逃走ルートを推測するロジックなど）
- スマホ実機向けのレイアウト調整（今回は明確にスコープ外とされている）

## 9. GitHub / バックアップの状態

**未確認。** 前回（Gemini）引き継ぎ時点では以下の状態だったと記録されているが、今回のClaudeセッションでの変更がコミット・プッシュされたかどうかは不明。

- Obsidian（`01_siogao`）: GitHubでの自動同期・バックアップ運用中（Gemini記録時点）
- Project Chaseコード（`project-Chase`）: 正常動作版の保存（コミット＆プッシュ）完了（Gemini記録時点）
- ローカル復旧用コピー：`ProjectChase_ClaudeRecovery` 保持済み（Gemini記録時点）

今回Claudeが変更した3ファイル（3章）が上記リポジトリへ反映・コミットされたかどうかは、本ファイル更新時点で**情報なし**。次のAIは、作業開始前に必ずGitHubの状態を確認すること。

## 10. 次のAIが最初に行うべきこと

次のAIは、いきなりコードを変更しない。

1. `AI_HANDOVER.md`（本ファイル）を読む
2. `AI_GUIDE.md` / `AI_PROJECT_MANAGER.md` / `CURRENT_STATUS.md` / `ROADMAP.md` を確認する
3. 直近に別のAIチャットで修正された内容がないか、ユーザーへ確認する
4. 現在のコード（特に `lib/views/log_panel.dart` / `board_widget.dart` / `game_page.dart`）と `TASKS.md` / `CHANGELOG.md` の整合性を確認する
5. GitHub / バックアップ状態を確認する
6. 8章の「ローカル2人プレイモード」について、しおがおさんに具体的な仕様（操作方法・画面遷移・ヘリ操作の割り当てなど）を確認し、推測で実装を始めない
7. 未確認の修正がある場合は、**その内容を聞いてから実装に入る**
8. 必要なら変更案（影響ファイル・変更内容）を先に提示し、承認を得てから変更する

## 11. 次のAIへの注意事項

- 既存仕様（ゲームルール・判定ロジック）を勝手に変更しない
- 指示された作業範囲を超えて変更しない
- 大幅なコード変更を一度に行わない
- 不明点を推測して実装しない。特に8章の2人プレイモードは仕様が固まっていないため、必ず事前にすり合わせる
- 動作確認が取れていないコードを「完成版」と判断しない
- 正常動作確認済みの状態を、バックアップなしで直接上書き破壊しない
- リファクタリングを理由にゲームの挙動・ルール・UIを変更しない
- しおがおさんのプロジェクトでは「新しい指示に対しては、まず復唱→確認→承認を得てからコーディングに着手する」運用ルールが徹底されている。今回のClaudeセッションでも、修正依頼のたびに意図をすり合わせてから着手する形が繰り返され、特に「捜索済みマーカーの仕様」は一度で意図が伝わらず、複数回のすり合わせを経て確定した経緯がある。次のAIも、認識のズレを早期に確認する姿勢を継続すること

## 12. 既存ドキュメントとの役割分担

- ゲーム仕様 → `SPECIFICATION.md`
- プロジェクト全体の状態 → `CURRENT_STATUS.md`
- 決定事項 → `DECISIONS.md`
- 作業履歴 → `DEVLOG.md`
- 変更履歴 → `CHANGELOG.md`
- タスク → `TASKS.md`
- 長期計画 → `ROADMAP.md`
- AIへの基本ガイド → `AI_GUIDE.md`
- AIによるプロジェクト管理方針 → `AI_PROJECT_MANAGER.md`
- **直前の作業状態・AI交代時の引き継ぎ → `AI_HANDOVER.md`（本ファイル）**

## 13. AI引き継ぎ手順

1. 現在のAIが作業終了
2. `AI_HANDOVER.md` を最新状態に更新
3. 必要に応じて関連ドキュメント（`CURRENT_STATUS.md` / `TASKS.md` / `DEVLOG.md` / `CHANGELOG.md` / `ROADMAP.md` 等）を更新
4. コード変更前のバックアップ（GitHubコミット等）を確認
5. 次のAIへプロジェクト一式と `AI_HANDOVER.md` を渡す
6. 次のAIは `AI_HANDOVER.md` および関連ドキュメントを確認する
7. 現在の状態を理解してから作業を開始する
